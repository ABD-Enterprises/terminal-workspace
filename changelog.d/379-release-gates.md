- [INTERNAL] Release gates: the updater .sig is verified against the pubkey compiled into the app and latest.json must describe the exact tarball (promote and publish, dry-run included); the five version sources must agree (validate + release check); publish refuses a tag that differs from the promoted version and runs every content gate before the GitHub release is created. (#379)
