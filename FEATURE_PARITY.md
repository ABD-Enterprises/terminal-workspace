# Feature Parity Matrix

Research date: 2026-03-29 (last full research pass)
Honest-scoring overlay added: 2026-04-27
Last audited against current code: 2026-05-06 (overlay bullets + row 46 reverified line-by-line)

Goal: build a local-first macOS replacement that reaches roughly 90% of the competitor daily-use value without cloning branding or chasing cloud/team features first.

> **Honest-scoring overlay (last audited 2026-05-06)**
>
> The presence-based "Implemented" column below over-counts daily-use parity. Scoring the same matrix on quality, default-safety, and friction lands at **~80% practical parity** (up from ~70% in 2026-04-27), not 96%. See:
> - `docs/parity-and-hardening-review.md` — independent review with file:line citations.
> - `docs/parity-and-hardening-plan.md` — phased remediation plan.
>
> Bullets below have been reverified on 2026-05-06; entries that turned out to be stale on the 04-27 pass have been rewritten with the current state.
>
> - **Command palette** — functional. `apps/desktop/src/components/layout/AppShell.tsx:629-1003` renders six populated sections (Active session, Recent, Sections, Sessions, Hosts, Snippets) with fuzzy search and ↑/↓/Enter navigation, exercised by `tests/e2e/command-palette.spec.ts`. Commands are inlined in `AppShell.tsx` rather than registered through a separate registry abstraction; that's a structural choice, not a missing feature. (Earlier "registry is empty" claim was stale; closed issues #23 and #25 came from that line.)
> - **Tabs and split panes** — tabs are reorderable via drag-and-drop (`apps/desktop/src/components/terminal/TerminalTabView.tsx:14-70`); the 2-pane split layout has a draggable splitter with mouse + keyboard nudge (`apps/desktop/src/components/terminal/SplitLayout.tsx:24-126`). The 04-27 "not reorderable / no drag resize" claim was stale on the bonus parity round.
> - **Passphrase + known hosts** — default `hostKeyPolicy` is `requireTrusted`; Node + Rust backends defense-in-depth refuse to connect without a known key. The first-connect fingerprint UX is an inline `<Modal>` from `apps/desktop/src/components/common/FingerprintTrustPrompt.tsx:1-17` (P2-FP shipped). Earlier "still a separate-page workflow" claim was stale.
> - **Import/export config** — SSH config import has the richer parser (`Host *` defaults, `ProxyJump`, multi-host lines) plus `Match host/originalhost/user/all` directives shipped 2026-05-06 (`apps/desktop/src/lib/ssh-config.ts:51-336`, PR #29). `Include` directive is the remaining gap and is split out to issue #28; it requires a backend filesystem primitive.
> - **Local prefs** — terminal theme picker with `prefers-color-scheme` Auto mode is shipped (`apps/desktop/src/routes/SettingsPage.tsx:381-489`); the remaining gap is the **font picker** (font family + size). The 04-27 "no theme picker, no prefers-color-scheme" claim was stale.
>
> Phase 0 hardening landed 2026-04-27 and Phase 1 UX bonus round (tabs/splits/fingerprint/themes) landed before 2026-05-06. See `docs/parity-and-hardening-plan.md` for what's queued next.

Primary competitor evidence:
- [Organizing data](https://competitor.com/documentation/organizing-data)
- [What is competitor?](https://www.competitor.com/documentation/what-is-competitor)
- [SSH keys](https://competitor.com/documentation/ssh-keys)
- [Secure credentials sync](https://competitor.com/documentation/secure-credentials-sync)
- [Port forwarding](https://competitor.com/documentation/port-forwarding)
- [competitor X: tabs and command palette](https://competitor.com/blog/competitor-x)
- [Broadcast input across multiple terminals](https://competitor.com/blog/broadcast-input)
- [Pricing and plan comparison](https://www.competitor.com/pricing)
- [Background sessions and history](https://support.competitor.com/hc/en-us/articles/900006226306-Keep-your-competitor-sessions-alive-in-the-background)

Original presence-based parity estimate (2026-03-29): 96%
Honest daily-use parity estimate (2026-04-27): ~70% — see review document.
Honest daily-use parity estimate (2026-05-06): ~80% — palette / tabs / splits / fingerprint / themes already shipped; remaining gaps are SSH config Include support (#28), keychain-prompt UX polish, font picker, native Tauri/Rust validation pass.

| Feature | User value | competitor behavior summary | Our implementation approach | Priority | Status | Evidence |
|---|---|---|---|---|---|---|
| Host inventory: add/edit/delete | Store real infrastructure targets in one place | competitor models remote machines as Hosts instead of loose connection strings, with metadata attached to the host record | Implemented local-first host CRUD with persisted Zustand state and editable host metadata | Must-have | Implemented | competitor docs: [What is competitor?](https://www.competitor.com/documentation/what-is-competitor); local UI/store: `apps/desktop/src/routes/HostsPage.tsx`, `apps/desktop/src/store/hosts-store.ts`; validated 2026-03-29 via build, tests, and Playwright |
| Groups, tags, favorites, search/filter | Fast retrieval across medium and large fleets | competitor highlights nested groups, labels/tags, and search as core navigation aids | Implemented groups, tags, favorites, sidebar search, and page-level filters; no inherited protocol settings yet | Must-have | Implemented | competitor docs: [Organizing data](https://competitor.com/documentation/organizing-data); local UI: `apps/desktop/src/components/hosts/HostFilterBar.tsx`, `apps/desktop/src/components/layout/Sidebar.tsx`; Playwright smoke included host creation and updated counts |
| Dense desktop shell, left nav, top tabs | Replace single-pane terminal apps with a navigable desktop workspace | competitor X moved to horizontal tabs plus a lighter sidebar to separate data from active work | Implemented routed shell with compact desktop density, left navigation, top tabs, persistent section shortcuts, and reduced-scroll operator panels across hosts, sessions, keys, snippets, and transfers | Must-have | Implemented | competitor blog: [competitor X](https://competitor.com/blog/competitor-x); local shell: `apps/desktop/src/components/layout/AppShell.tsx`, `apps/desktop/src/components/layout/TopTabs.tsx`, `apps/desktop/src/components/layout/Sidebar.tsx`; browser validation on 2026-03-29 confirmed the dense host grid and session workspace fit major controls and detail panels into a 1440x900 viewport |
| Command palette | Keyboard-first navigation across hosts and active work | competitor exposes a command palette for fuzzy host search and tab/action jumps | Implemented section jumps, active-session focus, host actions for session launch, SFTP, and trust workflows, plus snippet execution from the palette across route changes | Should-have | Implemented | competitor blog: [competitor X](https://competitor.com/blog/competitor-x); local palette and shortcuts: `apps/desktop/src/components/layout/AppShell.tsx`, `apps/desktop/src/components/layout/TopTabs.tsx`, `apps/desktop/src/hooks/useCommandPalette.ts`, `apps/desktop/src/routes/KeysPage.tsx`; validated 2026-03-29 with Playwright palette flows for `/transfers`, `/keys?scanHost=...&autoScan=1`, and route-hop snippet execution into `/tmp/termsnip-palette-snippet.log` |
| SSH connect/disconnect | Real replacement value starts when the app can open live SSH sessions | competitor supports SSH as a primary protocol with reconnect/history behavior | Implemented a local Node bridge with `ssh2`, websocket terminal IO, reconnect flow, and real local sshd validation; native Tauri/Rust backend still pending | Must-have | Implemented | Local runtime: `apps/desktop/server/backend.mjs`, `apps/desktop/src/components/terminal/TerminalPane.tsx`; validated 2026-03-29 against local sshd on `127.0.0.1:2222` plus browser reconnect smoke |
| Multiple tabs and split panes | Work comfortably across many hosts at once | competitor uses horizontal tabs, session switching shortcuts, and split view workflows | Implemented persisted tab and split-pane workspace around xterm.js panes with live SSH transport | Must-have | Implemented | competitor blog: [competitor X](https://competitor.com/blog/competitor-x); local state/UI: `apps/desktop/src/store/sessions-store.ts`, `apps/desktop/src/components/terminal/TerminalTabView.tsx`, `apps/desktop/src/components/terminal/SplitLayout.tsx`; validated 2026-03-29 in browser |
| Key import and generation | Remove dependence on external SSH config handwork | competitor provides key generation/import and host-to-key assignment | Implemented local key inventory, import by path, local `ssh-keygen` generation, and host assignment | Must-have | Implemented | competitor docs: [SSH keys](https://competitor.com/documentation/ssh-keys); local runtime/UI: `apps/desktop/server/backend.mjs`, `apps/desktop/src/routes/KeysPage.tsx`, `apps/desktop/src/store/keys-store.ts`; validated 2026-03-29 by importing `/tmp/termsnip-sshd/client_key`, generating `/tmp/termsnip-generated/ui_generated`, and assigning the generated key to `Billing API` |
| Passphrase handling and known hosts | Make private-key auth and trust prompts manageable in-app | competitor includes passphrase-aware keys plus trust/known-hosts UX | Implemented local known-host scanning, trust inventory, per-host strict trust policy, host-level revoke/reaccept flow, session-only runtime secrets, in-app missing-secret prompts for SSH/SFTP/snippet flows, and macOS Keychain persistence for host secrets, per-key-fingerprint passphrases, and per-identity passphrases via Tauri commands `termsnip_load/store/clear_*` | Must-have | Implemented | competitor docs: [SSH keys](https://competitor.com/documentation/ssh-keys); local auth/trust: `apps/desktop/src/types/host.ts`, `apps/desktop/src/routes/KeysPage.tsx`, `apps/desktop/src/routes/HostsPage.tsx`, `apps/desktop/src/store/known-hosts-store.ts`, `apps/desktop/src/store/connection-secrets-store.ts`, `apps/desktop/src/store/connection-secret-prompt-store.ts`, `apps/desktop/src/lib/runtime-secrets.ts`, `apps/desktop/src/lib/native-secrets.ts`, `apps/desktop/src/lib/connections.ts`, `apps/desktop/server/backend.mjs`, `src-tauri/src/main.rs`, `src-tauri/src/keychain_support.rs`, `crates/desktop-core/src/services/keychain_service.rs`; validated 2026-03-29 by importing a passphrase-protected localhost key, assigning it to `Local SSH Test B`, hard-reloading the app, satisfying the in-app passphrase prompt, and executing `PASSPHRASE_PROMPT_OK` over SSH; keychain integration verified 2026-05-06 (closed issue #25 was based on stale claim) |
| SFTP browser and transfer actions | File movement is part of daily remote work, not an edge feature | competitor includes SFTP as a first-class protocol and surfaces it prominently in navigation | Implemented remote browser, upload/download, rename/delete/create-folder, and a visible local transfer queue over the same SSH bridge | Must-have | Implemented | Local runtime/UI: `apps/desktop/server/backend.mjs`, `apps/desktop/src/routes/TransfersPage.tsx`, `apps/desktop/src/components/sftp/FileBrowser.tsx`, `apps/desktop/src/store/transfers-store.ts`; validated 2026-03-29 against local sshd fixture at `/tmp/termsnip-sftp-fixture` with Playwright and direct API calls |
| Snippets and multi-host execution | Save repetitive commands and operational playbooks | competitor offers snippets, startup snippets, and multi-execution workflows | Implemented a persisted snippet library with CRUD, default target hosts, run-in-active-pane injection, and direct multi-host SSH exec for broadcast execution | Must-have | Implemented | competitor pricing/blog: [Pricing](https://www.competitor.com/pricing), [Broadcast input](https://competitor.com/blog/broadcast-input); local UI/runtime: `apps/desktop/src/routes/SnippetsPage.tsx`, `apps/desktop/src/store/snippets-store.ts`, `apps/desktop/src/components/snippets/SnippetEditor.tsx`, `apps/desktop/src/components/snippets/SnippetList.tsx`, `apps/desktop/src/lib/api.ts`, `apps/desktop/server/backend.mjs`; validated 2026-03-29 with real localhost SSH execution into `/tmp/termsnip-snippet-active.log` and two-host broadcast into `/tmp/termsnip-snippet-broadcast.log` |
| Port forwarding | Replace ad hoc `ssh -L/-R` use with visible local management | competitor includes guided port forwarding and tunneling flows | Implemented live local and remote forwards scoped to SSH sessions, with active-forward management inside the session workspace | Must-have | Implemented | competitor docs: [Port forwarding](https://competitor.com/documentation/port-forwarding); local runtime/UI: `apps/desktop/server/backend.mjs`, `apps/desktop/src/components/terminal/PortForwardPanel.tsx`, `apps/desktop/src/lib/api.ts`; validated 2026-03-29 with local forwarding on `127.0.0.1:19090 -> 127.0.0.1:18081` and remote forwarding on `127.0.0.1:19091 -> 127.0.0.1:18081`, including teardown checks |
| Session restore | Resume interrupted work after relaunch | competitor keeps history and emphasizes staying productive across session interruptions | Implemented persisted tabs/panes with dead backend ids scrubbed, reconnect intent preserved across non-user disconnects, background restore for non-active SSH tabs, explicit `needs secrets` state when runtime credentials are missing after relaunch, and xterm lifecycle hardening so restores and route changes do not crash the workspace | Must-have | Implemented | competitor support: [Background sessions](https://support.competitor.com/hc/en-us/articles/900006226306-Keep-your-competitor-sessions-alive-in-the-background); local persistence/runtime: `apps/desktop/src/store/sessions-store.ts`, `apps/desktop/src/components/terminal/TerminalPane.tsx`, `apps/desktop/src/components/terminal/SessionRestoreManager.tsx`; validated 2026-03-29 by refreshing `/sessions`, auto-restoring an active localhost pane, background-restoring a second localhost tab with two split panes, executing `printf 'MULTI_RESTORE_OK\\n' > /tmp/termsnip-multi-tab-restore.log`, then validating a passphrase-protected relaunch path that surfaced `ssh · needs secrets`, resumed via `Resume SSH session`, and executed `PENDING_RESTORE_RESUME_OK` |
| Jump hosts / host chain | Common real-world access path for production fleets | competitor supports jump host / host chain configuration per pricing matrix and host model docs | Implemented one-hop jump-host selection in host records plus recursive backend connection building across exec, SFTP, and live interactive terminal sessions | Should-have | Implemented | competitor docs/pricing: [What is competitor?](https://www.competitor.com/documentation/what-is-competitor), [Pricing](https://www.competitor.com/pricing); local model/runtime: `apps/desktop/src/types/host.ts`, `apps/desktop/src/components/hosts/HostEditor.tsx`, `apps/desktop/src/lib/connections.ts`, `apps/desktop/server/backend.mjs`, `apps/desktop/src/components/terminal/TerminalPane.tsx`; validated 2026-03-29 with backend exec `JUMP_EXEC_OK`, raw websocket session execution `JUMP_WS_OK`, UI terminal execution `JUMP_UI_OK`, and transfer browsing through the bastion |
| Quick connect and duplicate session | Reduce friction when reopening a host or branching work into another tab | competitor emphasizes fast host launch and session branching inside the main workspace | Implemented searchable quick-connect actions inside the sessions sidebar with reuse-vs-new-tab behavior, plus duplicate-tab controls that keep titles readable as `Host`, `Host (2)`, `Host (3)` | Should-have | Implemented | competitor blog/docs: [competitor X](https://competitor.com/blog/competitor-x), [What is competitor?](https://www.competitor.com/documentation/what-is-competitor); local workspace/store: `apps/desktop/src/components/terminal/TerminalWorkspace.tsx`, `apps/desktop/src/store/sessions-store.ts`, `apps/desktop/src/types/session.ts`; validated 2026-03-29 in Playwright by filtering quick connect to `Local Env Agent Validation`, confirming reuse held the tab count at `2`, quick-connect `New` raised it to `3` with `Local Env Agent Validation (2)`, and `Duplicate tab` raised it to `4` with `Local Env Agent Validation (3)` |
| SSH agent forwarding and per-host environment variables | Advanced session ergonomics for power users | competitor surfaces agent forwarding and environment variables in paid plans | Implemented dense host-editor controls, persisted per-host env maps plus agent-forwarding flags, backend propagation across direct and jump-host SSH connections, and a shell/exec fallback that exports env vars even when the remote sshd does not accept arbitrary `AcceptEnv` values | Should-have | Implemented | competitor pricing: [Pricing](https://www.competitor.com/pricing); local model/runtime: `apps/desktop/src/types/host.ts`, `apps/desktop/src/components/hosts/HostEditor.tsx`, `apps/desktop/src/routes/HostsPage.tsx`, `apps/desktop/src/lib/connections.ts`, `apps/desktop/server/backend.mjs`; validated 2026-03-29 by creating `Local Env Agent Validation` in the live UI, confirming `agent` and `2 env vars` in the dense host inventory/details pane, writing `ENV_OK` and `shell` through a websocket SSH session into `/tmp/termsnip-env-shell.log` and `/tmp/termsnip-env-mode-shell.log`, and confirming forwarded key visibility through both `/tmp/termsnip-agent-shell.log` and snippet exec stdout |
| Local preferences for shell density and keyboard routing | Let power users keep the workspace dense without hard-coding one behavior for everyone | competitor exposes settings around interaction and presentation while keeping keyboard workflows prominent | Implemented persisted shell preferences for compact vs comfortable density plus a global section-shortcut toggle that controls shell badges and `⌘1` through `⌘6` routing | Should-have | Implemented | Local store/UI: `apps/desktop/src/store/app-store.ts`, `apps/desktop/src/components/layout/AppShell.tsx`, `apps/desktop/src/components/layout/Sidebar.tsx`, `apps/desktop/src/components/layout/TopTabs.tsx`, `apps/desktop/src/routes/SettingsPage.tsx`; validated 2026-03-29 with `app-store.test.ts`, Settings toggles, reload persistence in `window.localStorage.getItem('termsnip-app')`, and browser snapshots showing shortcut badges hidden when disabled |
| Import/export config | Migration and backup path away from competitor | competitor exposes vault-based storage and sync/export-oriented workflows | Implemented local JSON export/import for hosts, keys, snippets, and known-host trust with secret fields excluded and session reset on import | Should-have | Implemented | competitor storage model: [Organizing data](https://competitor.com/documentation/organizing-data), [Pricing](https://www.competitor.com/pricing); local runtime/UI: `apps/desktop/src/lib/local-config.ts`, `apps/desktop/src/routes/SettingsPage.tsx`; validated 2026-03-29 with `local-config.test.ts`, Settings export download, and Settings import of `/tmp/termsnip-import-config.json` into `Imported Fixture Host` |
| Cloud sync, shared vaults, team collaboration, terminal sharing | Important for organizations, not required for a 90% single-user local replacement | competitor emphasizes vaults, shared vaults, sync, and collaboration in docs/pricing | Explicitly deferred until local-first desktop workflows are strong | Deferred | Deferred | competitor docs/pricing: [Organizing data](https://competitor.com/documentation/organizing-data), [Secure credentials sync](https://competitor.com/documentation/secure-credentials-sync), [Pricing](https://www.competitor.com/pricing) |

## Premium-Oriented Features We Are Not Chasing First

- Personal and team vault sync
- Shared vaults and organization access control
- Real-time collaboration and terminal sharing links
- Mobile pairing, biometric cloud sync flows, and enterprise SSO

## Next Highest-Value Gap

Updated 2026-05-06 against current code:

1. ~~optional macOS Keychain integration for runtime secrets~~ — **shipped**: per-host, per-key-fingerprint, and per-identity passphrase persistence via Tauri keychain commands (`apps/desktop/src/lib/native-secrets.ts`, `src-tauri/src/main.rs`, `keychain_support.rs`).
2. validate the desktop shell against a native Tauri/Rust package path once `cargo` is available in this environment — still pending (validate.sh skips the cargo-based native trust step on machines without Rust installed).
3. ~~add theme customization~~ — **shipped**: terminal theme picker with `prefers-color-scheme` Auto mode (`SettingsPage.tsx:381-489`). Font picker (family + size) is the remaining gap.
4. ~~OpenSSH-config import polish~~ — **partially shipped**: Match host/originalhost/user/all in PR #29 (2026-05-06). Include directive support tracked in #28 (needs backend filesystem primitive).
5. cloud sync / E2E vault sync — design ticket #26 in `ready_for_plan` (multi-month design problem; do not implement until the seven open questions in the ticket are answered).
