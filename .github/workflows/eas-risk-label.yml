# Generated by ai-pipeline adopt --with-workflows from enterprise-ai-standards@v7.0.1 — overwrite via 'ai-pipeline adopt --force --with-workflows'.
# Do not hand-edit; tracked drift will be detected by 'ai-pipeline adopt' on the next run.
name: EAS Risk Label
on:
  pull_request:
    types: [opened, reopened, synchronize]

# Matches reusable/risk-label.yml's declared permissions.
permissions:
  contents: read
  pull-requests: write
  issues: write

jobs:
  call:
    uses: deffenda/enterprise-ai-standards/.github/workflows/reusable/risk-label.yml@v7.0.1
    with:
      heuristics_config_path: .github/eas-risk-heuristics.yml
    secrets: inherit
