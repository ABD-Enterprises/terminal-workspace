# Generated by ai-pipeline adopt --with-workflows from ABD-Enterprises/orc@eccc56ea331222ca79d900334679f30a5f24a468 — overwrite via 'ai-pipeline adopt --force --with-workflows'.
# Do not hand-edit; tracked drift will be detected by 'ai-pipeline adopt' on the next run.
# Thin referencing stub — shared logic lives in ABD-Enterprises/orc/.github/workflows/reusable/orc-superseded-check.yml@eccc56ea331222ca79d900334679f30a5f24a468.
name: ORC superseded-by check
on:
  pull_request_target:
    types: [closed]

permissions:
  contents: read
  pull-requests: write
  issues: read

jobs:
  call:
    uses: ABD-Enterprises/orc/.github/workflows/reusable/orc-superseded-check.yml@eccc56ea331222ca79d900334679f30a5f24a468
    with:
      runner_labels: ${{ vars.ORC_RUNNER_LABELS || '["ubuntu-latest"]' }}
    secrets: inherit
