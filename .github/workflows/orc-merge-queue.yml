# Generated by orc adopt --with-workflows from ABD-Enterprises/sure-forge@v8.2.0 — overwrite via 'orc adopt --force --with-workflows'.
# Do not hand-edit; tracked drift will be detected by 'orc adopt' on the next run.
# Thin referencing stub — shared logic lives in ABD-Enterprises/sure-forge/.github/workflows/orc-merge-queue.yml@v8.2.0.
name: ORC Merge Queue
on:
  pull_request:
  merge_group:

permissions:
  contents: read

jobs:
  call:
    uses: ABD-Enterprises/sure-forge/.github/workflows/orc-merge-queue.yml@v8.2.0
    with:
      runner_labels: ${{ vars.ORC_RUNNER_LABELS || '["ubuntu-latest"]' }}
    secrets: inherit
