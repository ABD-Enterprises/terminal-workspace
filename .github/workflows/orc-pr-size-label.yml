# Generated by orc adopt --with-workflows from ABD-Enterprises/sure-forge@v8.2.0 — overwrite via 'orc adopt --force --with-workflows'.
# Do not hand-edit; tracked drift will be detected by 'orc adopt' on the next run.
# Thin referencing stub — shared logic lives in ABD-Enterprises/sure-forge/.github/workflows/orc-pr-size-label.yml@v8.2.0.
name: ORC PR size label
on:
  pull_request_target:
    types: [opened, synchronize, reopened, edited]

permissions:
  contents: read
  pull-requests: write
  issues: write

jobs:
  call:
    uses: ABD-Enterprises/sure-forge/.github/workflows/orc-pr-size-label.yml@v8.2.0
    with:
      runner_labels: ${{ vars.ORC_RUNNER_LABELS || '["ubuntu-latest"]' }}
    secrets: inherit
