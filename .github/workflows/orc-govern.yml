# Generated by orc adopt --with-workflows from ABD-Enterprises/sure-forge@v8.2.0 — overwrite via 'orc adopt --force --with-workflows'.
# Do not hand-edit; tracked drift will be detected by 'orc adopt' on the next run.
# Thin referencing stub — shared logic lives in ABD-Enterprises/sure-forge/.github/workflows/orc-govern.yml@v8.2.0.
name: ORC Govern
on:
  pull_request_target:
    types: [opened, reopened, synchronize, ready_for_review, labeled, unlabeled]
  pull_request_review:
    types: [submitted, dismissed, edited]

permissions:
  actions: read
  checks: read
  contents: read
  issues: write
  pull-requests: write
  statuses: read

jobs:
  call:
    uses: ABD-Enterprises/sure-forge/.github/workflows/orc-govern.yml@v8.2.0
    with:
      runner_labels: ${{ vars.ORC_RUNNER_LABELS || '["ubuntu-latest"]' }}
    secrets: inherit
